"use client";
import { AppShell } from "@/components/app-shell";
import { api } from "@/lib/api";
import type { Bus } from "@/lib/types";
import { BusFront, Pencil, Plus, Trash2, X } from "lucide-react";
import { useEffect, useState } from "react";
const blank=():Bus=>({id:crypto.randomUUID(),bus_number:"",vehicle_number:"",route_name:"",driver_name:"",conductor_name:"",status:"active"});
export default function BusesPage(){const [buses,setBuses]=useState<Bus[]>([]),[draft,setDraft]=useState<Bus|null>(null),[error,setError]=useState("");const load=()=>api.buses().then(setBuses).catch(()=>setError("Start the FastAPI server to manage buses."));useEffect(()=>{void load()},[]);const save=async()=>{if(!draft)return;try{const exists=buses.some(b=>b.id===draft.id);await(exists?api.updateBus(draft):api.addBus(draft));setDraft(null);void load()}catch(e){setError(e instanceof Error?e.message:"Could not save bus")}};const remove=async(id:string)=>{if(!confirm("Delete this bus?"))return;try{await api.deleteBus(id);void load()}catch(e){setError(e instanceof Error?e.message:"Could not delete bus")}};return <AppShell><div className="mx-auto max-w-5xl"><section className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="label text-blue-300">Fleet directory</p><h1 className="mt-2 text-3xl font-bold tracking-tight">Your buses.</h1><p className="mt-2 text-sm text-slate-400">Manage the vehicles behind every collection sheet.</p></div><button onClick={()=>setDraft(blank())} className="btn-primary"><Plus size={17}/> Add bus</button></section>{error&&<p className="mt-5 text-sm text-rose-300">{error}</p>}<div className="mt-7 grid gap-4 sm:grid-cols-2">{buses.map(bus=><div className="card p-5" key={bus.id}><div className="flex justify-between"><span className="grid size-11 place-items-center rounded-xl bg-blue-400/10 text-blue-300"><BusFront size={21}/></span><div className="flex gap-1"><button className="btn-ghost p-2" onClick={()=>setDraft(bus)}><Pencil size={15}/></button><button className="btn-ghost p-2 text-rose-300" onClick={()=>remove(bus.id)}><Trash2 size={15}/></button></div></div><p className="mt-5 text-lg font-bold">{bus.bus_number}</p><p className="mt-1 text-sm text-slate-400">{bus.vehicle_number} · {bus.route_name}</p><div className="mt-5 flex items-center justify-between border-t border-white/[.07] pt-4 text-xs"><span className="text-slate-500">{bus.driver_name} · {bus.conductor_name}</span><span className="rounded-full bg-emerald-400/10 px-2 py-1 font-semibold text-emerald-300">{bus.status}</span></div></div>)}</div>{draft&&<div className="fixed inset-0 z-40 grid place-items-center bg-black/60 p-4 backdrop-blur-sm"><div className="card w-full max-w-lg p-6"><div className="flex items-center justify-between"><h2 className="text-lg font-bold">{buses.some(b=>b.id===draft.id)?"Edit bus":"Add a bus"}</h2><button onClick={()=>setDraft(null)} className="text-slate-500"><X/></button></div><div className="mt-5 grid gap-4 sm:grid-cols-2">{(["bus_number","vehicle_number","route_name","driver_name","conductor_name"] as (keyof Bus)[]).map(k=><label key={k} className={k==="route_name"?"sm:col-span-2":""}><span className="label">{k.replace("_"," ")}</span><input className="field mt-1.5" value={String(draft[k])} onChange={e=>setDraft({...draft,[k]:e.target.value})}/></label>)}<label><span className="label">Status</span><select className="field mt-1.5" value={draft.status} onChange={e=>setDraft({...draft,status:e.target.value as Bus["status"]})}><option value="active">Active</option><option value="maintenance">Maintenance</option><option value="inactive">Inactive</option></select></label></div><button disabled={!draft.bus_number||!draft.vehicle_number} onClick={save} className="btn-primary mt-6 w-full">Save bus</button></div></div>}</div></AppShell>}
