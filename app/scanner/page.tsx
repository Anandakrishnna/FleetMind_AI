"use client";
import { AppShell } from "@/components/app-shell";
import { api } from "@/lib/api";
import type { Bus, ExpenseKey, SheetInput } from "@/lib/types";
import { expenseLabel, money } from "@/lib/utils";
import { AlertTriangle, CheckCircle2, FileImage, Loader2, Save, ScanLine, Upload } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
const keys:ExpenseKey[]=["diesel","oil","tyre","spare_parts","workshop","stand_fee","washing","others"];
export default function ScannerPage(){const [buses,setBuses]=useState<Bus[]>([]),[data,setData]=useState<SheetInput>(),[file,setFile]=useState<File>(),[busy,setBusy]=useState(false),[saved,setSaved]=useState(false),[error,setError]=useState("");useEffect(()=>{api.buses().then(setBuses).catch(()=>setError("Start the FastAPI server before scanning."))},[]);const expense=useMemo(()=>data?Object.values(data.expenses).reduce((a,b)=>a+Number(b||0),0):0,[data]);const mismatch=data&&Math.abs(data.collection-expense-data.balance)>1; const scan=async()=>{if(!file)return;setBusy(true);setError("");try{setData(await api.extract(file));}catch(e){setError(e instanceof Error?e.message:"Could not scan image")}finally{setBusy(false)}};const update=(key:keyof SheetInput,value:string)=>setData(d=>d?{...d,[key]:key==="bus_id"||key==="driver_name"||key==="conductor_name"?value:Number(value)}:d);const save=async()=>{if(!data)return;setBusy(true);try{await api.saveSheet({...data,expense,balance:data.collection-expense});setSaved(true)}catch(e){setError(e instanceof Error?e.message:"Could not save")}finally{setBusy(false)}};return <AppShell><div className="mx-auto max-w-5xl space-y-7"><section><p className="label text-blue-300">AI collection scanner</p><h1 className="mt-2 text-3xl font-bold tracking-tight">From paper to performance.</h1><p className="mt-2 text-sm text-slate-400">Upload a handwritten sheet. FleetMind extracts every number into an editable record.</p></section>{saved?<div className="card p-10 text-center"><CheckCircle2 className="mx-auto text-emerald-400" size={44}/><h2 className="mt-4 text-xl font-bold">Collection sheet saved</h2><p className="mt-2 text-sm text-slate-400">Your dashboard has the latest fleet performance.</p><a className="btn-primary mt-6" href="/">View dashboard</a></div>:!data?<div className="card p-6 sm:p-10"><label className="block cursor-pointer rounded-2xl border border-dashed border-blue-400/35 bg-blue-400/[.04] px-6 py-12 text-center transition hover:bg-blue-400/[.08]"><input className="sr-only" type="file" accept="image/*" onChange={e=>{setFile(e.target.files?.[0]);setError("")}}/><span className="mx-auto grid size-14 place-items-center rounded-2xl bg-blue-500/15 text-blue-300">{file?<FileImage size={26}/>:<Upload size={26}/>}</span><p className="mt-5 font-semibold">{file?file.name:"Drop your collection sheet here"}</p><p className="mt-2 text-sm text-slate-500">PNG, JPG, or HEIC · max 10MB</p></label><button disabled={!file||busy} onClick={scan} className="btn-primary mt-5 w-full">{busy?<><Loader2 className="animate-spin" size={17}/> Reading sheet with AI…</>:<><ScanLine size={17}/> Extract collection details</>}</button>{error&&<p className="mt-4 text-sm text-rose-300">{error}</p>}<p className="mt-5 text-center text-xs text-slate-500">No API key? Demo extraction keeps the complete workflow running.</p></div>:<div className="grid gap-5 lg:grid-cols-[1fr_.85fr]"><div className="card p-5 sm:p-6"><div className="flex items-center justify-between"><div><p className="font-semibold">Review extraction</p><p className="mt-1 text-sm text-slate-500">Confidence {Math.round(data.confidence*100)}% · edit anything before saving</p></div><span className="rounded-full bg-emerald-400/10 px-3 py-1 text-xs font-semibold text-emerald-300">AI read complete</span></div><div className="mt-6 grid gap-4 sm:grid-cols-2"><Field label="Bus" value={data.bus_id} onChange={v=>update("bus_id",v)} select options={buses.map(b=>({value:b.id,label:`${b.bus_number} · ${b.vehicle_number}`}))}/><Field label="Driver" value={data.driver_name} onChange={v=>update("driver_name",v)}/><Field label="Conductor" value={data.conductor_name} onChange={v=>update("conductor_name",v)}/><Field label="Batha" value={data.batha} onChange={v=>update("batha",v)}/><Field label="Driver collection" value={data.driver_collection} onChange={v=>update("driver_collection",v)}/><Field label="Conductor collection" value={data.conductor_collection} onChange={v=>update("conductor_collection",v)}/><Field label="Checker collection" value={data.checker_collection} onChange={v=>update("checker_collection",v)}/><Field label="Total" value={data.total} onChange={v=>update("total",v)}/></div><p className="label mt-7">Expense breakdown</p><div className="mt-3 grid gap-3 sm:grid-cols-2">{keys.map(k=><Field key={k} label={expenseLabel(k)} value={data.expenses[k]||0} onChange={v=>setData(d=>d?{...d,expenses:{...d.expenses,[k]:Number(v)}}:d)}/>)}</div></div><aside className="space-y-5"><div className="card p-6"><p className="label">Validated totals</p><div className="mt-5 space-y-4"><Total label="Collection" value={data.collection}/><Total label="Expenses" value={expense} amber/><div className="border-t border-white/10 pt-4"><Total label="Calculated balance" value={data.collection-expense} strong/></div></div>{mismatch?<div className="mt-5 flex gap-3 rounded-xl border border-amber-400/25 bg-amber-400/10 p-3 text-sm text-amber-200"><AlertTriangle className="shrink-0" size={18}/>Possible calculation mismatch detected. You can still save after review.</div>:<div className="mt-5 flex gap-3 rounded-xl border border-emerald-400/20 bg-emerald-400/10 p-3 text-sm text-emerald-200"><CheckCircle2 className="shrink-0" size={18}/>Collection, expense, and balance are aligned.</div>}<button disabled={busy} onClick={save} className="btn-primary mt-5 w-full">{busy?<Loader2 className="animate-spin" size={17}/>:<Save size={17}/>} Save reviewed sheet</button>{error&&<p className="mt-3 text-sm text-rose-300">{error}</p>}</div><div className="rounded-2xl border border-blue-400/15 bg-blue-500/[.06] p-5"><p className="text-sm font-semibold text-blue-200">Why review matters</p><p className="mt-2 text-sm leading-6 text-slate-400">Every extracted value remains editable. Your approval is the final source of truth.</p></div></aside></div>}</div></AppShell>}
function Field({label,value,onChange,select,options}:{label:string;value:string|number;onChange:(v:string)=>void;select?:boolean;options?:{value:string;label:string}[]}){return <label><span className="label">{label}</span>{select?<select className="field mt-1.5" value={value} onChange={e=>onChange(e.target.value)}>{options?.map(o=><option className="bg-slate-900" key={o.value} value={o.value}>{o.label}</option>)}</select>:<input className="field mt-1.5" type={typeof value==="number"?"number":"text"} min="0" value={value} onChange={e=>onChange(e.target.value)}/>}</label>}
function Total({label,value,amber,strong}:{label:string;value:number;amber?:boolean;strong?:boolean}){return <div className="flex items-center justify-between"><span className={strong?"font-semibold":"text-sm text-slate-400"}>{label}</span><span className={`${strong?"text-xl font-bold text-emerald-300":amber?"text-amber-300":"font-semibold"}`}>{money(value)}</span></div>}
