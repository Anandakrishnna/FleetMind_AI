"use client";
import { motion } from "framer-motion";
import { ArrowDownRight, ArrowUpRight, BusFront, IndianRupee, ReceiptText, TrendingUp } from "lucide-react";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { useEffect, useState } from "react";
import { api } from "@/lib/api";
import type { Dashboard as DashboardData } from "@/lib/types";
import { money, shortDate } from "@/lib/utils";
const icons=[IndianRupee,ReceiptText,TrendingUp,BusFront];
export function Dashboard() { const [data,setData]=useState<DashboardData>(); const [error,setError]=useState(""); useEffect(()=>{api.dashboard().then(setData).catch(()=>setError("Start the FastAPI server to load your live fleet data."));},[]); if(error) return <div className="card p-8 text-slate-300">{error}</div>; if(!data) return <DashboardSkeleton/>; const metrics=[{label:"Today's revenue",value:data.metrics.revenue,delta:"12.4%",up:true},{label:"Today's expenses",value:data.metrics.expense,delta:"4.8%",up:false},{label:"Net profit",value:data.metrics.profit,delta:`${data.metrics.margin}% margin`,up:true},{label:"Active buses",value:data.metrics.bus_count,delta:"On route",up:true}]; return <div className="space-y-7"><section className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="label text-blue-300">Saturday · Live operations</p><h1 className="mt-2 text-3xl font-bold tracking-tight text-white sm:text-4xl">Good evening, Arun.</h1><p className="mt-2 text-sm text-slate-400">Here&apos;s how Malabar Transit is performing today.</p></div><a href="/scanner" className="btn-primary"><ReceiptText size={17}/> Scan collection sheet</a></section><section className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{metrics.map((m,i)=>{const Icon=icons[i]; return <motion.div key={m.label} initial={{opacity:0,y:12}} animate={{opacity:1,y:0}} transition={{delay:i*.06}} className="card p-5"><div className="flex items-start justify-between"><span className="label">{m.label}</span><span className="rounded-lg bg-blue-400/10 p-2 text-blue-300"><Icon size={16}/></span></div><p className="mt-5 text-2xl font-bold tracking-tight">{i===3?m.value:money(Number(m.value))}</p><p className={`mt-2 flex items-center gap-1 text-xs ${m.up?"text-emerald-400":"text-amber-400"}`}>{m.up?<ArrowUpRight size={13}/>:<ArrowDownRight size={13}/>} {m.delta} <span className="text-slate-500">vs yesterday</span></p></motion.div>})}</section><section className="grid gap-5 xl:grid-cols-[1.55fr_.8fr]"><div className="card p-5 sm:p-6"><div className="flex items-start justify-between"><div><p className="font-semibold">Revenue pulse</p><p className="mt-1 text-sm text-slate-500">Collection and expense over the last 7 days</p></div><span className="rounded-lg bg-white/[.05] px-3 py-1.5 text-xs text-slate-400">Weekly</span></div><div className="mt-7 h-64"><ResponsiveContainer width="100%" height="100%"><AreaChart data={data.chart}><defs><linearGradient id="revenue" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#58a6ff" stopOpacity=".35"/><stop offset="100%" stopColor="#58a6ff" stopOpacity="0"/></linearGradient><linearGradient id="expense" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#fbbf24" stopOpacity=".2"/><stop offset="100%" stopColor="#fbbf24" stopOpacity="0"/></linearGradient></defs><CartesianGrid vertical={false} stroke="#ffffff12"/><XAxis dataKey="date" axisLine={false} tickLine={false} tick={{fill:'#718096',fontSize:12}}/><YAxis hide/><Tooltip contentStyle={{background:'#0d192a',border:'1px solid #ffffff16',borderRadius:12}} formatter={(v:number)=>money(v)}/><Area type="monotone" dataKey="revenue" stroke="#58a6ff" strokeWidth={3} fill="url(#revenue)"/><Area type="monotone" dataKey="expense" stroke="#fbbf24" strokeWidth={2} fill="url(#expense)"/></AreaChart></ResponsiveContainer></div></div><div className="card overflow-hidden"><div className="border-b border-white/[.07] p-5"><p className="font-semibold">Fleet intelligence</p><p className="mt-1 text-sm text-slate-500">Signals worth your attention</p></div>{data.insights.map((insight,i)=><div key={insight.title} className="border-b border-white/[.06] p-5 last:border-0"><span className={`inline-flex rounded-full px-2 py-1 text-[10px] font-bold uppercase tracking-wider ${insight.kind==='success'?"bg-emerald-400/10 text-emerald-300":"bg-amber-400/10 text-amber-300"}`}>{insight.kind}</span><p className="mt-3 font-medium">{insight.title}</p><p className="mt-1 text-sm leading-6 text-slate-400">{insight.body}</p></div>)}</div></section><section className="card overflow-hidden"><div className="flex items-center justify-between border-b border-white/[.07] p-5"><div><p className="font-semibold">Recent collection sheets</p><p className="mt-1 text-sm text-slate-500">Your latest reviewed uploads</p></div><a href="/scanner" className="text-sm font-semibold text-blue-300">New scan</a></div><div className="overflow-x-auto"><table className="w-full min-w-[600px] text-left text-sm"><thead className="bg-white/[.025] text-xs uppercase tracking-wider text-slate-500"><tr><th className="px-5 py-3 font-medium">Bus</th><th className="px-5 py-3 font-medium">Date</th><th className="px-5 py-3 font-medium">Collection</th><th className="px-5 py-3 font-medium">Expenses</th><th className="px-5 py-3 font-medium">Balance</th></tr></thead><tbody>{data.recent.map(s=><tr key={s.id} className="border-t border-white/[.05]"><td className="px-5 py-4"><p className="font-medium">{s.bus_number}</p><p className="mt-0.5 text-xs text-slate-500">{s.vehicle_number}</p></td><td className="px-5 py-4 text-slate-400">{shortDate(s.service_date)}</td><td className="px-5 py-4">{money(s.collection)}</td><td className="px-5 py-4 text-amber-300">{money(s.expense)}</td><td className="px-5 py-4 font-semibold text-emerald-300">{money(s.balance)}</td></tr>)}</tbody></table></div></section></div> }
function DashboardSkeleton(){return <div className="space-y-7 animate-pulse"><div className="h-24 w-72 rounded-xl bg-white/[.06]"/><div className="grid gap-3 sm:grid-cols-4">{[1,2,3,4].map(x=><div key={x} className="h-36 rounded-2xl bg-white/[.06]"/>)}</div><div className="h-80 rounded-2xl bg-white/[.06]"/></div>}
